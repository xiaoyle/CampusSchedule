# 0.3.0 DIY 版验证说明

本版按用户要求，仅做必要构建和基础启动检查，交付用户自行测试。

- Gradle assembleDebug / lintDebug：通过；无编译或 lint 错误。
- UI 规范静态检查：通过，0 errors / 0 warnings。
- API 35 模拟器：新版覆盖安装成功，主页面及 DIY 页面已打开并查看截图。
- 真实图片导入、裁剪、主题收藏、不同启动器组件更新和布局：待用户真机测试，不作为已验证功能承诺。
- 原有课表与提醒逻辑未改动；此次没有重复执行 Word/PDF、多机型、提醒锁屏回归。
- 包名 cn.campus.schedule，版本 0.3.0，versionCode 4；签名沿用当前开发签名。
- 仅安卓 APK；不支持通过卓易通将安卓组件显示在原生鸿蒙桌面。

以下是旧版本历史记录，不能视为 DIY 版全量验证结果。

---

## 交付最终复测

- 最终 APK 构建与 Android Lint 完成，无错误；`.tools/widget-large-final-build.log`。
- 最终 220×180 / 300×300 组件在 1.5 倍字号、深色模式下通过四种数据状态测试；教室文字高度和署名均检查，截图人工确认；`.tools/widget-large-final-test.log`。
- 实际打开系统桌面 Widgets 列表，展开“中大课表助手”看见两个条目：下一节课 4×3、今日课程 4×4，并通过 Add 将下一节课放到桌面。证据：`.tools/screenshots/widget-picker-020.png`、`widget-home-020.png`。此处是 Android 15 模拟器默认桌面，不是华为真机。
- APK 签名校验成功、16 KB ZIP 对齐通过；原生库 ELF 对齐见 `.tools/elf-020.txt`。未运行 16 KB 模拟器，不以静态检查代替运行验证。
- UI 静态审计：0 错误、0 警告，`.tools/ui-audit-020.json`。

# 0.2.0 组件适配与闹铃验证（2026-09-07）

制作：**xiaoyle**。小范围同学试用 APK。

## 本次变更与边界

- 两个组件增加独立名称、标准预览与初始布局，明确内部安装；添加请求检查返回值，系统登记数量可在设置中查看。
- 补充华为“窗口小工具”和荣耀“经典小工具”路径；服务卡片和安卓小工具可能是分开的列表。
- 已知华为反馈是“桌面列表找不到”，具体型号、系统版本不明。没有对应真机，不宣称已复现或保证所有华为均已修复；原生鸿蒙兼容容器可能不向原生桌面提供安卓组件。
- 设置页和两个组件固定显示“xiaoyle 制作”。修正小尺寸在 1.5 倍字号下教室文字空间不足；按用户后续要求，下一节课默认 250×180dp、今日课程默认 250×220dp，两者可缩放下限为 220×180dp。
- 新增持续闹铃，默认开启，使用系统闹钟音量；通知操作或应用按钮停止，最长 60 秒自动停止。关闭开关后仅普通通知。立即试听、1 分钟后测试与取消可用。
- 提醒诊断分别读取应用通知、通知渠道、准时权限和电池优化；七组品牌指引可手动切换，排查信息只复制不发送。
- 学校登录按钮、Activity 声明及 INTERNET 权限从本版移除；保留 Word/PDF 文件导入。
- 无备份、恢复或迁移开发，不主动清空数据。

## 已取得的证据

| 验证范围 | 实际结果 |
|---|---|
| Kotlin 核心 | 13 项通过：原有日期、周次、Word/PDF、修改匹配，以及新增品牌映射 |
| API 35 导入与兼容检查 | 4 项通过：真实 Word/PDF 29 项安排、17 周；系统登记 2 个组件；学校 Activity 和联网权限缺席；设置品牌切换与安全回退；闹铃媒体播放状态和停止 |
| API 35 闹铃调度 | 2 项通过：1 分钟真实闹钟触发、重复替换、取消、自动停止；启动恢复逻辑清除测试，不改变课表 |
| API 35 PDF 确认保存 | 2 项通过：PDF 字节解析、预览、重复导入与损坏文件保护 |
| API 35 原生组件 | 初轮 180×140、300×300；后续提高下限为 220×180，两组件在未导入/上课/无课/结束状态渲染。最终字号/尺寸结果见下方交付复测 |
| 真机反馈 | 用户报告旧版在荣耀 GT / Android 16 可用；新版尚未逐项由用户复测 |

根据用户最新要求，本轮集中处理组件，停止扩大 API 26/31/33/36 和 16 KB 模拟器运行矩阵。最低 Android 8.0 是安装配置范围，不代表这些版本已全部实测。本地已有系统镜像不构成验证通过证据。

闹铃测试确认 MediaPlayer 播放状态、系统闹钟触发、停止和超时；模拟器静音运行，不能替代真机扬声器、勿扰和厂商省电下的听感/及时性测试。权限开启也不能保证长时间锁屏提醒准点。

证据：`.tools/test-020-35b.log`（8 项通过）、`.tools/test-020-35.log`（初轮组件通过及一次已修正的测试定位失败）、`.tools/test-020-35-font15-dark.log`（初轮自动检查通过但截图发现教室文字被裁切，已修正并增强文字高度断言），`.tools/widget-final-font15.log`（修正后通过）、后续组件最终复测日志及 `.tools/screenshots/`。原有历史结果如下。

# 0.1.1 PDF 导入验证（2026-09-07）

## 用户真机反馈（2026-09-07）

用户反馈：荣耀 GT / MagicOS 10 / Android 16 已成功完成所需使用任务。记录为用户报告的真机可用结果；没有独立逐项重跑锁屏待机、重启、拒绝权限等测试，不据此扩展为所有荣耀或安卓机型均通过。此前“尚未连接”描述指开发环境未连接此真机；学校登录完整链路也未因此单独确认。

- 新增 PDFBox Android 2.0.27.0 在本机提取文字和边框，按表格还原星期、跨页单元格和连续节次；复用 Word 课程合并逻辑。
- 提供的真实 PDF：9 页、29 项课程安排、11 个节次、覆盖 17 周。全部规则、周次、节次、教师、教室与原始 Word 对比一致（忽略 PDF 排版丢失的空格；选课人数不纳入课表模型）。
- 核心测试 11 项通过：包括 9 项原有测试、PDF 几何/完整对照/跨格式修改保留，以及缺页、无文字和非法起始日拒绝。
- 新增 2 项安卓仪器测试通过：实际读取 PDF 字节，验证日期/多教室/周末；导入预览确认、重复导入保留修改、损坏 PDF 不覆盖旧数据。
- 08:00 数学课程于 07:50 提醒的计算正确，连续至 09:40；线性代数保留 3 个候选教室，新生研讨课保留 24 个候选安排。
- 通过真实系统文件选择器，在 Downloads 中选中 PDF 并成功进入预览（content URI 读取）；重复导入摘要为新增 0、移除 0、更新 0。
- PDF 预览截图已检查；Android 15 模拟器覆盖安装成功。荣耀 GT / Android 16 真机文件选择与后台行为仍需用户安装后验证。
- 支持范围：有文字层和完整表格边框的中大课表 PDF。扫描件、加密/旋转页面和其他布局不宣称支持，不包含 OCR。
- 文件根据内容识别，系统文件选择器允许所有类型，避免聊天应用给 PDF 标错类型导致隐藏。原有 Word 导入继续保留。
- 学校 App 内登录与二次验证状态未改变，仍未经账号实测。

Android Lint：0 错误，16 项提示（原有兼容性/JavaScript/Kotlin 写法，加上集合写法建议）。

证据：`.tools/pdf-final-build.log`、`.tools/pdf-final-device-tests.log`、核心测试 XML 和 `.tools/screenshots/pdf-preview.png`。私有课表只进入测试 APK，不进入交付 APK 或源码包。

以下为 0.1.0 的原始验证记录（历史结果）：

# 安卓第一版验证记录

验证日期：2026-09-06；交付整理日期：2026-09-07。安装包版本：0.1.0。目标个人设备：荣耀 GT / MagicOS 10 / Android 16，尚未连接。

## 已验证

| 项目 | 结果 |
|---|---|
| Kotlin 核心测试 | 9 项通过；含 5 项真实课表相关/周次测试和 4 项合成输入测试 |
| 安卓模拟器仪器测试 | 6 项通过，Android 15 / API 35，x86_64 |
| 空页面、设置、课程详情与周课表 | 实际启动并检查截图 |
| 真实 Word 文件在安卓上解析 | 成功；包含重复 tcPr、横向与纵向合并单元格 |
| 导入预览与确认 | 确认前不写入；确认后保存第 1 周 2026-09-07 |
| 修改保留、重复导入与冲突 | 核心与设备测试通过 |
| 下一节课、今日课程组件 | 原生 AppWidgetHost 实际渲染，并检查截图 |
| 定时通知 | 在模拟器中设定未来触发时间，AlarmManager 实际投递通知 |
| 重启恢复 | 模拟器重启并解锁后，无需打开应用，已重建组件边界刷新与次日 07:50 的课前提醒 |
| 通知未授权路径 | 初始设备测试中不崩溃，课表可继续使用 |
| 深色模式与字号 1.3 倍 | 今日界面实际截图检查，长地点正常换行 |
| APK 安装 | 模拟器安装成功 |
| APK 签名 | apksigner 校验成功，v2 开发签名 |
| Android Lint | 无错误；保留新版本属性兼容提示、学校 WebView 启用 JavaScript 的审查提示和 Kotlin 写法建议 |
| 设计检查 | DESIGN.md 校验 0 错误、0 警告；UI 静态审计无发现 |

日期验收：第 1 周周一为 2026-09-07，第 9 周周一为 2026-11-02；周一高等数学 08:00–09:40，提前 10 分钟对应 07:50。没有将第 9 周排除的课程错误地按全年每周重复。

重启路径实现：BOOT_COMPLETED、应用升级、系统时间和时区变更、精确闹钟授权变更均直接重建调度，并保留 WorkManager 恢复备用。最终重启实测中，系统日志确认由 RecoveryReceiver 启动应用进程，AlarmManager 中恢复两个待执行任务；没有通过手动打开界面触发恢复。

## 学校登录核查

读取了学校公开门户、本科教务入口和课表前端资源；从公开源码确认导出采用表单 POST，地址路径为 `/timetable-search/stuTimeTabPrint/output`。只对真实同源导出表单捕获，不猜测私有登录接口，不绕过二次验证。

**未使用用户账号登录，因此“App 内登录 → 二次验证 → 课表查询 → 导出”的完整链路尚未通过验收。** 当前界面明确标注待实测，提供已验证的 Word 文件导入。公开页面资源和表单观察不能代替登录实测。

## 仍需在荣耀 GT 上检查

- 从手机文件管理器选取学校课表，以及桌面启动器的添加、缩放和点击效果。
- 锁屏、待机、省电模式、重启后的提醒及时性；勿扰与通知声音设置。
- 学校账号登录、二次验证、手机上的导出按钮是否存在，以及导出捕获是否兼容。
- 其他安卓厂商、Android 8–14 和 Android 16 真机均未逐台验证；不能据模拟器结果声称全部兼容。

苹果与鸿蒙原生版本属于后续阶段。本版没有自动刷新学校课表、云同步或自动节假日停课。

## 证据位置

本工作目录的 `core/build/test-results/test/`、`app/build/reports/lint-results-debug.html`、`.tools/final-device-tests.log`、`.tools/screenshots/` 保存开发验证输出。它们不进入可分享源码包，避免混入个人课表样本或本机缓存。源码中保留测试代码；私有输入不分发。


## 网页版 0.1.0 · 2026-09-08

Windows / Edge 自动检查通过：原始九页 PDF 导入（29 项/17 周），Word 与浏览器 PDF 每条展开课程及候选信息一致；预览确认、保存后重开、损坏文件不覆盖、离线基础壳/已保存课表、离线 PDF 再导入、安装引导、320/390/768/1280 宽度无整页横向溢出。浅色未导入页面及深色设置页已检查截图。离线通过浏览器网络模拟验证；网络状态标识依赖 navigator.onLine，不能用于判断学校网站是否可达。

待真机：iOS 17+ Safari、添加到主屏幕、独立启动、系统字号、横竖屏与安全区域、学校认证/二次验证、快捷指令权限和完整导入；Android 手机浏览器安装。Windows Edge 结果不作为这些项目已通过的证据。
