---
version: alpha
colors:
  primary: "#176B52"
  background: "#F3F7F4"
  foreground: "#172D26"
  darkBackground: "#14251E"
  darkPrimary: "#91D6B4"
  darkForeground: "#E2F2E9"
  container: "#D8EBE1"
  navigation: "#EAF1EC"
  darkContainer: "#244C3C"
  darkNavigation: "#1C3026"
typography:
  heading:
    fontFamily: "sans-serif"
    fontSize: "30px"
  body:
    fontFamily: "sans-serif"
    fontSize: "16px"
rounded:
  card: "18px"
  featured: "24px"
spacing:
  screen: "20px"
  card: "16px"
omitted:
  - section: components
    reason: "Native Material 3 components, described below"
---

# 中大课表助手设计规范

## 0.14.0 · 邀请制课业社区

- 主导航为“今日、计划、社区、我的”。社区使用与本地页面一致的深绿强调色，帖子卡突出标题、三行摘要、作者和互动数量。
- 未登录浏览不弹登录墙；只有发布、收藏、评论、举报等写操作才引导登录。首次打开且未配置服务时显示部署说明和明确的 HTTPS 地址入口。
- 本地笔记发布采用独立副本确认页，明确列出上传内容；社区编辑、删除、注销和举报均使用原因清晰的确认反馈。
- 网络加载只在社区局部显示进度，离线缓存标为“当前显示离线缓存”；失败时保留本地课表、笔记和缓存，不用全屏异常文字替代内容。
- 社区 Markdown 当前以安全正文展示，不执行 HTML、脚本或远程图片；链接策略由服务端限制为 HTTPS。

## 0.11.0 交互补充

- 全局短提示使用圆角悬浮卡片、主题表面色和轻阴影，避开纯黑横条；错误提示使用错误容器色与错误图标，普通成功提示使用主题主色图标。
- 笔记遵循“栏目筛选—卡片浏览—专注编辑”的三层结构。默认清爽纸张，可切换方格或暖纸风格；点缀色只用于栏目、边条和焦点，保证正文对比度。
- 笔记标题最多 80 字，正文最多 20000 字；编辑未保存时返回提供保存、放弃和继续编辑。

## 0.12.0 · 沉浸笔记与流畅首页

- 笔记使用“列表—阅读—编辑”层级。阅读和编辑页面隐藏主导航，固定顶部返回与保存，把剩余高度完整交给正文。
- 默认纯文本保留低门槛，Markdown 是逐篇开启的可选模式；阅读器只解释有限排版语法和 HTTPS 链接，不加载图片、HTML 或脚本。
- 纸张纹理由 Compose 轻量绘制，五种纸张与排版参数共用 `NoteAppearance`。全局样式是默认值，单篇覆盖不影响其他笔记。
- 今日页的课程、任务和未来负荷由单个后台快照产生。时间变化只刷新需要显示倒计时的卡片，滚动过程中不得重新解析课表、读取数据库或解码头像。

## Overview
面向中山大学学生的安卓个人工具，以赶往教室前的一眼阅读为核心。采用课表与教学楼门牌的清晰层级：大号时间、课程名、教室。深绿只强调正在发生和即将发生的课程。中文简体，日期按北京时间计算。本应用不是学校官方产品。

这是产品界面，不使用营销横幅、学校校徽或登录页仿制。根据用户已确认的方案开发，所有私有课表样本仅用于本机测试，不打包到安装包和源码分发。

本文件记录规范，`app/src/main/kotlin/cn/campus/schedule/Theme.kt` 的 Palette 是运行时颜色来源，Compose 和 Glance 共同消费。上述 px 设计数值在安卓中分别映射到 sp（文字）和 dp（尺寸）。

## Colors
浅色主题使用 primary / background / foreground；深色主题对应 darkPrimary / darkBackground / darkForeground。错误和冲突使用 Material 3 error 色，并始终配文字。保留系统权限与学校网页的原生外观。

## Typography
系统中文无衬线字体，无外部字体下载。时间使用 32sp 粗体，页面标题 30sp，正文使用 Material 3 body，说明文字使用 bodySmall。完整课程名和教室允许换行，桌面组件受尺寸限制最多两行，点击查看完整详情。尊重系统字号和屏幕阅读器。

## Layout
主界面三栏底部导航：今日、计划、我的。“计划”内部切换日历和待办，“我的”集中课表与提醒、个性化、资料、复盘和成就入口。页面边距 20dp；课程卡片内边距 16dp；列表自然滚动。今日页面先呈现个人问候、下一步和开始专注，再展示学期进度、课程与任务。每周课表按星期一到星期日纵向排列，避免长课程名在手机网格中难以阅读。

## Elevation & Depth
平面、细边框卡片；下一节课使用主色调容器区分。使用 Material 3 原生对话框表达临时操作，不叠加装饰阴影。

## Shapes
课程卡片 18dp，重点卡片 24dp，桌面 20dp。按钮采用 Material 3 形状与原生触控反馈。

## Components
统一 LessonCard 用于今日和周列表。无课、未导入、学期结束、已取消、待确认地点、真实冲突都有文字状态。导入忙碌时显示线性进度并禁止再次提交。保存使用同一反馈渠道，解析失败保留原课表。预览取消不产生写入。

所有操作使用 Compose 原生 Button、TextButton、RadioButton、可访问的 Card 等组件；图标来自 Material Icons，独立图标按钮有中文内容描述。仅使用原生状态反馈动画。

## Do's and Don'ts
- 优先显示真实时间、地点、待处理问题。
- 多教室保留候选，并提供本次选择，不替用户猜测。
- 不显示学生姓名、学号、密码或验证码，不把私有样本当作默认课程。
- 不宣称未验证的手机后台提醒或学校登录已经通过。

## PDF import update · 0.1.1
文件入口统一接受 Word / PDF，不新增重复页面。使用同一导入预览、增删改摘要和确认保存操作；解析失败不修改旧数据。明确说明先把聊天文件保存到手机，PDF 扫描件不可导入。

## 多机型适配 · 0.2.0
保持现有绿色 Palette 与系统字体。提醒检查按具体系统状态逐项呈现，不使用“全部安全”总分。品牌通过原生 Material DropdownMenu 切换，文字说明自然换行，设置页由 LazyColumn 统一滚动。署名“xiaoyle 制作”固定于两个组件底部，11sp；用户允许放大组件：下一节课默认 250×180dp，今日课程默认 250×220dp，下限 220×180dp；紧凑布局采用 4dp 外边距，避免大字号挤掉地点；组件正文使用剩余空间，小尺寸先显示时间、课程名和地点。普通通知测试、延时测试和持续闹铃试听分别命名，停止闹铃始终可从设置页操作。学校登录入口本版移除。


## 桌面 DIY · 0.3.0
由用户选择照片作为视觉主体，编辑器沿用 Palette 和 Material 3 系统中文字体。顶部固定布局预览、下部设置滚动、底部固定保存。照片卡使用渐变暗遮罩和白字；毛玻璃对选图做降采样模糊并加浅透明内面板；便签使用 #F5EEDC / #FAF5E5 纸色、#E6EEDC 课程底色、#24352E 正文。点缀色由 DiyStyle 统一定义为薄荷 #B7F2D5、晴蓝 #BEE2FF、莓粉 #FFD1DF、暖杏 #FFE2A8。DiyRenderer 为预览和 Glance 提供同源背景渲染，前景颜色来自 DiyStyle.foreground/highlight。用户可调透明度，需通过预览和手机自行确认与壁纸的对比度。
图片不进入源码或发布包；没有默认人物图。系统字体适应现有应用；字号仅调整课程正文，署名固定 11sp。未保存变化返回时提示；两个组件草稿独立。

## 网页导入 · 0.4.0

保持 Palette 与系统字体。“从学校网页导入”为设置页主操作，文件导入使用轮廓按钮。学校页面保持学校原有样式；应用自有底栏使用 surfaceContainer，包含当前域名、持久文字状态、固定高度进度区、全宽导入按钮及次要操作。网页占剩余空间，底栏和键盘互不遮挡，状态使用可访问 live region；网页忙碌不改变按钮名称。返回取消本次读取，清除登录使用 Material 3 确认对话框。与文件入口共用预览和保存，不新增第二套课程确认界面。


## 0.4.1 · 认证窗口修复

学校网页底栏增加返回教务登录和复制排查信息，沿用 Material 3 与现有 Palette；当前子窗口占用原网页区域。


## 0.4.2 · 手机版课表导入

按钮保持“导入当前课表”，底栏分别显示切换完整课表、读取、失败提示；不新增导出对话框。

## 网页版 / PWA 0.1.0

网页版是同仓库独立 `web/` 工程。仅中文简体、北京时间；深绿强调上课时间和教室。`web/src/style.css` 的全局变量是 Web 颜色来源，与安卓 Palette 对齐。今日重点卡采用课表门牌式时间栏，其他卡片保持安静。系统字体，标题 25–34px，正文 16px；课程名换行，无横向整页滚动。安全区域用 env()，页面高度使用 dvh 并保留 vh 回退。

布局：品牌和离线状态 → 页面标题 → 下一节课/周列表/设置 → 固定底部三项导航。原生 date/select/文件输入是 Web 表单统一控件；Modal 使用原生 dialog，支持键盘关闭与焦点管理。按用户要求，不把原生安卓背景 DIY 带入本阶段。

深色模式、可见焦点、44px 以上触控目标、系统滚动条、减少动画偏好属于网页设计约束。主屏幕安装教程、导入预览和错误恢复与产品页面共用样式；快捷指令教程作为独立可离线文档。

## 个人化助手 · 0.5.0

运行时主题由 `CampusPalettes` 统一提供康乐园绿、珠海海蓝及春夏秋冬配色，应用与默认组件共用；组件自己的 DIY 点缀色拥有更高优先级。课程颜色以 6dp 色条和时间文字表达，冲突与取消仍依靠文字，不只依赖颜色。

个人中心顶部使用按北京时间月份切换的原创 Canvas 展示图，资料卡、主题卡和启动卡使用 22–24dp 圆角。头像固定预留尺寸，缺图时显示昵称首字。组件底部署名左对齐、头像右对齐，不挤占课程正文。

启动页以深色电影感场景承托居中芯片，按住时环形进度连续增长，提前松手回退，完成后淡入主界面。系统减少动画时减少景深、粒子和位移。内置雕像、日月、窗边主题均为代码绘制原创场景；女性人物明确表现为成年大学生。桌面组件只使用静态画面。

## 校园照片展示栏 · 0.5.1

个人中心顶部使用 1200×720 的中大校园实景照片，卡片高度 204dp、圆角 26dp。每个阶段提供三张图片并允许水平分页；右上角胶囊圆点显示当前位置。底部使用由透明到 82% 黑色的渐变承托白色标题和每日短句，避免照片明暗变化损害可读性。默认页按北京时间日期轮换，用户仍可左右滑动。短句不超过 20 个字符，共 128 条。

## 课业助手与学习看板 · 0.6.0

待办页采用时间范围分组，顺序为逾期、今天、本周、以后、无截止日期、已完成。任务卡左侧沿用课程色，紧急程度同时显示文字标签；课程失联、逾期和完成状态均使用明确文案。已完成分组默认折叠，清空前要求确认。

今日页首屏使用头像、昵称和当前教学周建立个人语境，用线性进度显示学期进度。下一节课保留最高视觉权重，并增加实时倒计时；课程数量、待办数量和最近截止项使用紧凑信息块，避免与课表正文竞争。

新增学习看板组件，尺寸与课程组件一致，优先显示最近任务、截止状态、今日课程数量与完成进度；头像位于右下角，`xiaoyle 制作` 固定于左下角。无任务、全部完成和未导入课表时仍保留统计与明确状态。组件不增加图片背景设置，跟随全局主题和深色模式。

## 智能学习日历 · 0.7.0

课表页顶部使用“周课表／月历”双模式切换并记住选择。月历采用七列等宽日期格，课程与任务分别用小圆点标记；逾期使用错误色，全部完成使用第三强调色，同时保留日期、数量和文字状态，避免只凭颜色判断。

所选日期下方使用纵向时间轴混排课程与任务。今日页增加“下一步”卡和七天负荷条，个人中心的轻量成长卡只显示周目标、完成率和连续进展，不使用排名、扣分或失败动画。

任务编辑器使用系统日期、时间选择器并保留格式输入。重复范围、结束日期、子任务清单和“仅本次／整个系列”保持同一表单层级；删除整个系列使用二次确认。任务搜索和筛选默认折叠，减少首屏密度。

## 专注光轨与成就馆 · 0.8.0

专注页采用深色全屏校园昼夜光轨。太阳或月亮沿弧线移动，弧线长度表达当前进度，中央只保留目标、时间和暂停／结束操作。减少动态效果开启时停止粒子和轨道位移，保留静态进度、文字和操作。专注完成先呈现结果，再由用户决定完成任务、继续或休息。

成就徽章采用校园珐琅章视觉：深绿、海蓝、珊瑚红、金与银作为有限色板，锁定状态使用压印轮廓和明确进度。解锁动画约 1.2 秒，可跳过；精选区最多三枚，避免个人主页变成密集徽章墙。自定义编辑器始终同时显示彩色与锁定预览，颜色之外保留名称、图标及无障碍说明。

学习复盘以事实数据为主：大号专注分钟、每日趋势、课程分布、任务完成率和未来负荷。图表同时显示数字或文字，不以单一颜色表达完成、逾期或课程归属。作者栏目使用清晰的外部链接、复制和拨号盘动作，不自动发送或拨号。

## 0.9.0 · 课程系列与未来芯核

- 自建课程编辑区使用明确的“仅这一次／每周重复”选择，重复次数只在每周模式出现。
- 系列摘要必须同时显示星期、时间、总次数和首末日期；冲突提示列出具体日期且不能阻止用户保存。
- 已存在系列的范围选择固定为“仅本次／本次及以后／整个系列”，危险删除动作使用文字说明作用范围。
- 三套启动图采用统一的高质未来科技视觉，中央为六边形芯核保留空间，底部保持低细节以保证文字对比度。
- 启动动态仅包含不超过 2% 的景深位移、少量粒子和光轨；减少动态效果时画面静止。
- 场景选择必须展示缩略图，并允许在个人中心内直接预览完整长按交互。


## 0.10.0 视觉更新

个人中心轮播采用四组原创横向无人风景：动漫校园、末世废墟、赛博空间与温馨校园。画面不叠加字幕，风格入口固定在右上角 48dp 热区，页码放在底部。启动芯核允许主色与光环分别设置，按钮文字按底色亮度自动选择黑白。三个组件沿用同一裁剪和遮罩规则，但分别保存外观。
