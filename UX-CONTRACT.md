# 交互约定

## 0.12.0 笔记流程

- 点击已有笔记先进入阅读页；编辑按钮进入完整编辑页；新建直接进入编辑页。阅读、编辑和外观设置期间隐藏底部主导航。
- 编辑内容在停止输入后暂存草稿，正式保存后清除。存在未保存变化时返回必须提供保存、放弃和继续编辑，不能静默丢失输入。
- 默认外观影响所有未单独设置的笔记；开启单篇外观后，全局调整不得覆盖该笔记。关闭单篇设置后立即恢复跟随全局。
- Markdown只作为可选内容模式。无法识别的语法按普通文字显示，远程图片显示为已忽略提示。

依据：本任务用户确认的《中大课表助手：安卓第一版》方案；用户提供的 Word XML 课表；用户确认第 1 周为 2026-09-07。

| 操作 | 进行中 | 成功 | 失败与恢复 |
|---|---|---|---|
| 文件导入 | 限制文件 8 MB，显示进度 | 预览周次、课程数、变化及待确认项 | 原课表保留；提示重新选择 |
| 确认导入 | 禁止重复提交 | 原子替换课表、保留匹配修改、刷新提醒与桌面 | 保留预览和原数据；如预览期间数据已变更需重新导入 |
| 修改本次课程 | 日期、时间格式检查 | 更新原始课程日期对应的一次安排 | 校验失败保留字段 |
| 取消本次课程 | 可逆修改 | 在列表标记已取消，不提醒 | 通过“恢复学校原始安排”恢复 |
| 多地点选择 | 显示原始候选 | 仅确认本次课程，提醒仍为一次 | 未选择保留“待确认” |
| 提醒授权 | 系统拥有权限弹窗 | 显示当前授权状态 | 不阻止离线课表；说明延迟或无法通知 |
| 学校登录 | 由真实学校网页处理 | 导出成功返回导入预览 | 兼容性待实测；允许退回文件入口 |
| 新增单次课程 | 校验日期、名称和时间 | 写入后刷新课表、组件与提醒 | 冲突明确提示但允许保存；字段错误保留输入 |
| 删除课程 | 应用内确认动作 | 导入课程只取消当天，自建课程移除 | Snackbar 可撤销；已取消导入课程可从详情恢复 |
| 个人资料 | 用户主动填写并保存 | 仅保存在私有设置 | 可一键清除；不进入网页登录、课表或排查信息 |
| 启动充能 | 按住约 1.5 秒显示连续进度 | 轻触反馈后进入主界面 | 提前松手回退；组件、通知及关闭设置时跳过 |
| 新增/编辑待办 | 校验标题、截止时间和提醒关系 | 保存后重排分组、刷新首页和组件、重建该任务提醒 | 保留输入并显示错误；旧数据不变 |
| 自定义待办提醒 | 选择具体日期与时间，换算为相对截止时间的偏移 | 使用真实待办渠道重建系统闹钟；可即时或延时测试 | 时间晚于截止、缺少截止或超过一年前会提示修正 |
| 灵感笔记 | 栏目筛选后新建或编辑，输入状态保留 | 标题、正文、置顶和外观原子保存到本机 | 未保存返回时提供保存、放弃和继续编辑；失败不覆盖旧内容 |
| 完成/恢复待办 | 立即更新本地状态 | 完成时取消提醒，恢复后按有效设置重建 | 操作后提供撤销入口 |
| 删除/复制待办 | 删除只作用于当前任务；复制打开编辑 | 删除可撤销；副本保留标题、课程和备注且提醒关闭 | 学校课程删除不连带删除待办 |

首版只保存一份当前学期课表。导入不同学期同样展示替换和未匹配修改清单。日期不从文件名推断，保存用户确认的周一。节假日不自动变更。文件中的姓名不入库；账号无自建后端；应用备份关闭。清除学校会话前说明会退出登录，已导入课表保留。

系统文件选择器、权限页和 WebView 内学校页面属于系统或学校界面；应用自有页面使用一致的中文 Material 3 组件。安卓原生产品不适用 DOM、CSS、浏览器滚动条或网页路由检查。

## 0.2.0 用户确认的变更
本版关闭学校登录入口及其 Activity 声明，不提供跳转；只保留文件导入。增加提醒检查、多品牌手动切换和仅复制的排查信息；不能读取的厂商设置标注手动检查。系统设置打开失败先回退应用详情，再失败显示手动说明。1 分钟测试独立于课程安排，重复替换、可取消、不跨重启恢复。课程闹铃默认开启，按系统闹钟音量持续播放，用户停止或 60 秒到期自动结束，关闭后保留普通通知；不更改音量或绕过勿扰。无备份恢复或数据迁移开发。组件请求已提交不等于已添加；显示系统登记状态并提供华为窗口小工具/荣耀经典小工具指引。


## 桌面 DIY · 0.3.0
依据：用户同意自选背景、三种样式、图片裁剪与模糊、个性标题及主题保存，并要求只做必要检查后提供 APK 自测。

| 能力 | 统一实现 | 行为 |
|---|---|---|
| DIY 页面 | DiyActivity / Material 3 | 由设置页进入，返回原页面；返回前提示未保存更改 |
| 图片选择 | 系统 OpenDocument / DiyStore | 取消不修改；只导入选中的图片，最大 24 MB，异步解码并压缩到最长 1600px；失败保留草稿 |
| 背景渲染 | DiyRenderer | 预览与桌面共用裁剪、模糊、遮罩、纸纹与透明度 |
| 外观保存 | DiyStore / SharedPreferences | 两类组件独立保存，刷新现有 Glance；失败显示错误，课表和提醒不受影响 |
| 交互反馈 | Material 3 Snackbar / AlertDialog | 处理时禁止重复操作；删除收藏提供撤销 |
| 图片定位 | 预览拖动 / Material Slider | 拖动有水平、垂直和缩放滑块替代操作 |
| 主题收藏 | DiyStore | 最多 8 套，显式收藏和应用；收藏立即保存，应用后点击保存到桌面 |

DIY 不宣称解决卓易通与鸿蒙桌面之间的组件兼容性。此次不做多机型回归，真实图片、启动器显示和交互由用户测试补充。

## 网页导入 · 0.4.0

依据：用户确认的 0.4.0 网页登录一键导入方案；本节取代 0.2.0 关闭学校登录的当前行为约定。

| 能力 | 统一实现 | 行为 |
|---|---|---|
| Date / Form | 设置页 OutlinedTextField / LocalDate | 沿用 YYYY-MM-DD，进入网页前检查周一，网页导入使用同一日期 |
| 导入 / CRUD | MainViewModel.preview / confirmImport | 两种入口共用预览、差异、保存、取消和原子替换 |
| 状态 / Toast | 主界面 Snackbar；网页登录底栏文字状态 | 长任务、错误和恢复文字留在底栏，可访问 live region；禁止重复点击 |
| 导航与滚动 | SchoolBrowserActivity / WebView | 学校页面独立滚动，应用底部固定操作栏；不注入学校样式 |
| 登录清除 | CookieManager / WebStorage / Material AlertDialog | 清除学校登录、网页缓存和历史，不更改课程或外观 |
| 身份与数据生命周期 | docs/PRIVACY.md / SchoolImportCache | 仅私有规范化结果缓存，令牌一次消费，过期清理；不读取登录表单 |

横竖屏变化保留网页；系统销毁 Activity 后保留 Cookie，重新加载登录入口，未完成导入需重试。预览未保存前不改变课程；页面变化、会话过期、网络/证书错误、单周页面均不得写入旧课表。


## 0.4.1 · 认证窗口修复

子窗口共享统一 WebView 配置；返回优先关闭子窗口，恢复父窗口。原始 POST 不转换为 GET，不自动定时销毁认证页面。诊断仅包含版本、域名与分类错误码。


## 0.4.2 · 手机版课表导入

教务域名内非电脑版课表入口点击导入会打开已知完整课表路由；自动导入有超时限制、重复提交保护，返回/清除会话时取消等待。直接读取完整表格后复用预览保存。

## 网页版 / PWA 0.1.0

Web 的组件所有权：课程卡为 main.tsx 的 card；对话框为 Modal；日期及周选择为原生 date/select；表单校验由 model.ts 拥有；反馈为统一 message/notice，弹窗内也显示失败原因；滚动由 style.css 保留系统行为；持久写入仅由 store.ts 执行。

文件/粘贴 → 解析 → 预览 → 明确确认 → IndexedDB 写入完成 → 刷新界面。解析失败或取消不写入；忙碌锁阻止重复提交。新版可刷新提示在解析或预览时禁用，避免丢失未确认数据。快照仅接收全部周次。学校页面由 Safari/学校负责，不在 PWA 中仿造登录页。

推荐先安装再导入；Safari 与独立 Web App 数据不保证共享，用备份文件转移。更新 Service Worker 不删除 IndexedDB。学校快捷指令输出经剪贴板显式带回，未授权时提供手动粘贴，不自动读取。

## 0.5.0 个人化与单次课程

课表页是单次课程 CRUD 的唯一入口，详情对话框是编辑和删除的统一入口。自建课程与学校课程共享冲突、组件和提醒计算，但不产生每周重复。个人中心是头像、资料、全局主题和启动场景的唯一设置入口；桌面 DIY 继续只负责两个组件的背景与排版。全局主题低于组件 DIY 点缀色优先级。

## 0.6.0 课业待办

待办页是任务新增、编辑、完成、恢复、复制和删除的统一入口；课程详情只提供带课程关联的新建入口。任务与课程通过稳定规则编号关联，同时保存课程名快照。重新导入后匹配到相同课程则继续关联，匹配失败时保留任务并显示“原课程已移除”，用户可在编辑页重新关联。

任务通知与课程提醒、测试提醒和持续闹铃使用独立渠道和标识。提醒默认关闭；无截止时间时不能开启。通知“完成”操作直接写入完成时间并取消后续提醒，“查看”打开对应任务且跳过启动充能页。设备重启、时间或时区变化只恢复未完成且未来有效的任务提醒。

## 0.7.0 学习日历与重复任务

周课表和学习月历共享课程、任务实例与日期状态。月历点击日期只改变当前查看日；快速新增会带入该日期。重复任务按需生成实例，不提前写入无限记录。完成、删除和子任务勾选只作用于当前实例；编辑和删除时可明确选择“仅本次”或“整个系列”，删除整个系列必须再次确认。

每日、每周、自选星期均以首次截止时间为锚点，结束日期包含当天。整个系列修改更新系列模板并保留已有实例完成记录。组件、通知和应用深链都传递任务编号与实例编号，避免完成错误日期。搜索同时匹配标题、课程和备注；清除筛选恢复全部状态。

### Canonical UI Map（Web）

## 0.8.0 专注、复盘与成就

今日页是开始专注的主要入口；课程和任务详情可带入关联目标。开始后活动计时状态成为唯一事实来源，界面、锁屏通知和学习看板读取同一状态。暂停不累计时长，结束少于 10 分钟时要求确认；专注结束不会自动完成关联任务。进程重建按墙上时间和单调时钟恢复，设备重启后采用安全恢复或暂停状态。

成就进度只从唯一专注记录和任务实例计算，重复保存、恢复或通知动作不能重复计数。内置成就一旦解锁永久保留；自定义成就修改条件前必须确认重置。自选徽章图片损坏时回退到模板图标，仍保留名称、条件和进度。最多三枚精选徽章，排序结果即时保存在本机。

“我的”是复盘、成就、设置、个性化、资料和作者信息的统一入口。GitHub、项目网站和问题反馈使用系统浏览器；微信只复制；手机号可复制或打开拨号盘，均不执行后台外发动作。

### Canonical UI Map（Web）

| Capability | Canonical owner | Source of truth | Allowed variants | Verification |
|---|---|---|---|---|
| Select/Listbox | main.tsx 原生 select | 本约定 | 教学周 | 浏览器选择与键盘 |
| Date | main.tsx 原生 date + model.monday | 本约定 | 第一教学周 | 日期边界测试 |
| Form | model.ts 校验 + run 忙碌锁 | 本约定 | 文件、粘贴 | 成功/损坏/重复 |
| Scrollbar | style.css 系统滚动条 | DESIGN.md | 页面、弹窗 | 320–1280 宽度 |
| Toast | main.tsx message/notice | 本约定 | 页面、弹窗内 | 错误及保存反馈 |
| CRUD | store.ts + 导入预览 | 本约定 | 确认替换 | 保存、重开、失败保留 |

## 0.9.0 course-series contract

- A recurring manual course expands lazily from its base, revisions, and per-instance edits; it is never stored as duplicated independent courses.
- Instance keys stay `manual@seriesId@index` after title, date, time, or location changes. Task and focus links retain the series ID.
- “Only this occurrence” writes one exception. “This and future” appends a revision at the selected index. “Entire series” rewrites the base and clears prior revisions and exceptions so the change applies to every occurrence.
- Deleting future occurrences truncates the series. Deleting from index zero removes the series. Destructive actions retain the existing snackbar undo path.
- Old manual-course JSON without repeat fields decodes as one occurrence. Old launch scene identifiers normalize to the new three scenes.



## 0.10.0 交互约定

系统返回优先关闭弹层和详情，再按访问顺序返回主栏目；首页第一次返回显示“再按一次退出”。输入错误保留原内容并说明修复方式。提交期间禁用重复点击；组件外观保存与刷新分别反馈。主页面使用 180ms 淡入，系统关闭动画时即时切换。新版速览每次安装数据仅显示一次。
