# 贡献与反馈

感谢关注中大课表助手。当前项目由 xiaoyle 个人维护，优先处理可复现的课表导入、桌面组件和提醒问题。

## 提交问题

请使用仓库的“问题反馈”模板，写明：

- 手机品牌、型号、Android / 系统版本和 App 版本；
- 是否通过卓易通等兼容环境运行；
- 清晰的复现步骤、实际效果和期望效果；
- 设置页“复制排查信息”，以及已遮住个人信息的截图。

不要公开上传学校账号、密码、验证码、姓名、学号、设备唯一标识或原始个人课表。课表解析问题可以先制作只保留结构的脱敏样本，并说明具体导出方式。

## 提交代码

1. 先创建 Issue 描述问题和预期行为，避免重复实现。
2. 保持课程解析与 Android UI 分离，通用规则放在 `core`。
3. 新解析规则应提供不含真实身份和课程隐私的最小测试样本。
4. 不加入账号收集、统计分析、广告、远程上传或未经说明的网络请求。
5. 提交前运行：

```bash
./gradlew :core:test :app:assembleDebug :app:lintDebug
```

Pull Request 请说明改动原因、用户可见行为、验证方式和仍未验证的设备范围。界面改动附脱敏截图。

## 兼容性反馈

厂商模拟器只能证明通用逻辑，不能替代对应品牌真机验证。请区分“模拟器通过”“真机反馈”和“尚未验证”。原生 HarmonyOS NEXT 与安卓兼容容器属于不同运行环境；安卓 Glance 组件无法视为鸿蒙服务卡片。

## 许可证

当前项目尚未指定源码许可证。在维护者明确添加许可证前，请先通过 Issue 联系维护者，不要默认复制、修改或再次分发源码。
